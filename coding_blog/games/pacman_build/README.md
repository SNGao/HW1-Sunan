# Pac-Man Game Build

This folder will contain the pygbag build of the Valentine's Pac-Man game.

## Building the Game

1. Navigate to the `Pac_Man` folder
2. Run: `pygbag --build main.py`
3. Copy the contents of `build/web` to this folder

The game will then be playable at `games/pacman.html`.
